<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Gestocom{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
		
		LISTE DES USAGERS</br></br>
		<table>
		{% for u in pUsagers %}
		<tr>
			<td><a href="{{ path('consulterUsager', { 'id': u.id }) }}">{{ u.nom }}</a></td>
			<td>{{ u.prenom }}</a></td>
	{% else %}
		<tr>
		<td>Aucun usager n'a été trouvé.</td>
		</tr>
	{% endfor %}
</table>
	 
    </body>
</html>